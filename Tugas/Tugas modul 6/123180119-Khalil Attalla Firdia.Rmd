---
title: "Modul6-Tugas"
author: "Khalil"
date: "12/5/2020"
output: pdf_document
---
1.) 
```{r}
  library(dslabs)
  library(dplyr)
  data("murders")
  murders <- mutate(murders, rate = total/population * 10^5 )
```

2.) 
```{r}
  x <- murders$rate
  murders <- mutate(murders, ranking = rank(x) )
```

3.) 
```{r}
  select(murders, state, abb)
```

4.) 
```{r}
  murders %>% filter( ranking > 46) %>% select(state,rate,ranking)
```

5.) 
```{r}
  murders %>% filter(region == "Northeast" | region == "West",rate < 1) %>% select(state,rate,ranking)
```

1.) 
```{r}
  my_state <- murders %>%
  mutate(rate = total/population * 10^5) %>%
  filter(region == "Northeast" | region == "West", rate < 1) %>%
  select(state,rate,ranking)

  my_state 
  
```
